import { Button } from '@/components/ui/button';

const Signin = () => {
  return (
    <div className="h-full overflow-y-auto flex justify-center items-center">
      <Button>Sign in with Airtable</Button>
    </div>
  );
};

export default Signin;
